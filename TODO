Break apart read_into_buffer
	Move input_error into it's own function

Break apart tokenize
	Move token_to_number into it's own function
	Move token_error into it's own function
	
In if-else tree perform EOL lookahead first.

Dry Code
